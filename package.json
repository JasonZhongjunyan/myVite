{
  "name": "myvite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm -C packages/admin --stream run dev",
    "dev:basic": "pnpm -C packages/basic --stream run dev",
    "dev:deer": "pnpm -C packages/deer --stream run dev",
    "dev:demo": "pnpm -C packages/demo --stream run dev",
    "build:admin": "pnpm -C packages/admin run build",
    "build:basic": "pnpm -C packages/basic run build",
    "build:deer": "pnpm -C packages/deer run build",
    "build:demo": "pnpm -C packages/demo run build",
    "preview:admin": "pnpm -C packages/admin --stream run preview",
    "preview:basic": "pnpm -C packages/basic --stream run preview",
    "preview:deer": "pnpm -C packages/deer --stream run preview",
    "preview:demo": "pnpm -C packages/demo --stream run preview"
  },
  "dependencies": {
  },
  "devDependencies": {
    "vite": "^4.1.0"
  }
}
